import React from "react";

const Title = () => {
    return (
        <header className={"header layout-content-container"}>
            <h1>Days of the Week</h1>
        </header>
    );
};

export default Title;
